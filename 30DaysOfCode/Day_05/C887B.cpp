#include <bits/stdc++.h>
using namespace std;

bool canMake(vector<vector<bool>> &dice, int n, int num)
{
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int n, temp;
    cin >> n;
    
    return 0;
}