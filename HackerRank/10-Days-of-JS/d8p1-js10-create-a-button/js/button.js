let btn = document.getElementById("btn");
function increase() {
    let currentVal = btn.innerHTML;
    btn.innerHTML = parseInt(currentVal) + 1
}

function increase() {
    let currentVal = btn.innerHTML;
    btn.innerHTML = parseInt(currentVal) + 1
}